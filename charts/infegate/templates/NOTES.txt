Infegate {{ .Chart.AppVersion }} is configured for {{ .Values.publicUrl }}.

LLM API: {{ .Values.publicUrl }}/v1
Management UI: {{ .Values.publicUrl }}/ui/

The API, UI, PostgreSQL, OIDC provider, TLS Secret, and runtime provider
credentials must be operated independently of this chart.
