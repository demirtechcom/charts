server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;

    location = /healthz {
        access_log off;
        return 200;
    }

    location /ui/ {
        try_files $uri /ui/index.html;
    }

    location / {
        return 404;
    }
}
